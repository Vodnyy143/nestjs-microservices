# Chat Microservices — NestJS Monorepo

```
                        ┌─────────────────┐
  Client (HTTP/WS) ───► │   API Gateway   │ :5000
                        └────────┬────────┘
                                 │ RabbitMQ (RPC + Events)
              ┌──────────────────┼───────────────────┐
              │                  │                   │
    ┌─────────▼──────┐  ┌────────▼───────┐  ┌───────▼────────┐
    │  auth-service  │  │ users-service  │  │  chat-service  │
    │  (JWT tokens)  │  │  (profiles)    │  │  (msgs + WS)   │
    └────────────────┘  └────────────────┘  └────────────────┘
            │                   │                   │
    ┌───────▼──────┐   ┌────────▼───────┐  ┌───────▼────────┐
    │  postgres    │   │   postgres     │  │   postgres     │
    │  (auth_db)   │   │  (users_db)    │  │   (chat_db)    │
    └──────────────┘   └────────────────┘  └────────────────┘
```

## 🚀 Быстрый старт
1. Поднять инфраструктуру
```bash
$ docker compose up -d
```